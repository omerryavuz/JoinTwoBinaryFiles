This program gets two input binary files,a output binary file,attribute name used to join and memory size.There are two different .java file that are designed with using Block Nested Loop Algorithm and Nested Loop Algorithm to merge the files.
